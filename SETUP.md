# VibeCode - Детальная инструкция по запуску

## 🚀 Быстрый старт (Docker)

### Предварительные требования

- Docker и Docker Compose установлены
- Порты свободны: 5173 (frontend), 8000 (backend), 5433 (postgres)

### Запуск одной командой

```bash
git clone https://github.com/chrizantona/t1.git
cd t1/deploy
docker-compose up --build
```

Готово! Открывайте http://localhost:5173

## 🔧 Конфигурация

### Переменные окружения

API ключ Cloud.ru уже настроен в `backend/app/core/config.py`. Если нужно изменить:

```python
# backend/app/core/config.py
SCIBOX_API_KEY: str = "ваш_ключ"
```

### Используемые модели (Cloud.ru API)

| Модель | Назначение |
|--------|------------|
| Qwen/Qwen3-235B-A22B-Instruct-2507 | Чат, генерация задач, оценка ответов |
| Qwen/Qwen3-Coder-480B-A35B-Instruct | Анализ кода, подсказки |
| openai/whisper-large-v3 | Распознавание речи |
| BAAI/bge-m3 | Эмбеддинги |

### Порты

| Сервис | Порт |
|--------|------|
| Frontend (Vite) | 5173 |
| Backend (FastAPI) | 8000 |
| PostgreSQL | 5433 |

## 📱 Использование платформы

### 1. Главная страница
- Выберите направление: ML Engineer / Data Scientist / ML Ops
- Выберите уровень: Junior / Middle / Senior
- Опционально: загрузите резюме для автоопределения уровня
- Нажмите "Начать интервью"

### 2. Страница интервью
- **Левая панель** — условие задачи и примеры
- **Центр** — Monaco Editor для написания кода
- **Правая панель** — чат с AI интервьюером

Возможности:
- 🎤 Голосовой ввод — нажмите микрофон, говорите, отпустите
- 💡 Подсказки — в боковой панели (⚙️), 3 уровня
- ▶️ Запуск кода — тестирование решения
- → Следующая задача — после успешного решения

### 3. Страница теории
- 5 вопросов по ML/DS
- Текстовые ответы оцениваются LLM
- Влияет на итоговый грейд

### 4. Страница результатов
- Итоговый грейд (Junior/Middle/Senior)
- Trust Score (анти-чит)
- Skill Radar — карта навыков по 5 осям
- Рекомендации для развития

## 🛠 Разработка без Docker

### Backend

```bash
cd backend

# Создать виртуальное окружение
python -m venv venv
venv\Scripts\activate  # Linux: source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить
uvicorn app.main:app --reload --port 8000
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

### PostgreSQL

Нужен PostgreSQL. Можно запустить только его через Docker:

```bash
docker run -d \
  --name vibecode-postgres \
  -e POSTGRES_USER=vibecode \
  -e POSTGRES_PASSWORD=vibecode \
  -e POSTGRES_DB=vibecode \
  -p 5433:5432 \
  postgres:15-alpine
```

## 📝 API Endpoints

### Interview
```
POST /api/interview/start              # Начать интервью
GET  /api/interview/{id}               # Получить интервью
GET  /api/interview/{id}/tasks         # Получить задачи
POST /api/interview/submit             # Отправить решение
POST /api/interview/chat               # Чат с AI
POST /api/interview/hint               # Запросить подсказку
POST /api/interview/{id}/generate-task # Сгенерировать задачу
GET  /api/interview/{id}/report        # Финальный отчёт
POST /api/interview/{id}/complete      # Завершить интервью
```

### Questions
```
GET  /api/questions/{interview_id}     # Получить вопросы
POST /api/questions/{id}/answer        # Ответить на вопрос
```

### Voice
```
POST /api/voice/transcribe             # Распознать речь (Whisper)
```

### Resume
```
POST /api/resume/analyze               # Анализ резюме
```

### Admin
```
GET  /api/admin/interviews             # Список интервью
GET  /api/admin/statistics             # Статистика
```

## 🐛 Troubleshooting

### Docker не запускается
```bash
# Проверить статус
docker ps

# Посмотреть логи
docker-compose logs -f
```

### Порт занят
```bash
# Windows
netstat -ano | findstr :5173

# Убить процесс
taskkill /PID <pid> /F
```

### Backend ошибки
```bash
docker-compose logs -f backend
```

### Голосовой ввод не работает
- Проверьте разрешение микрофона в браузере
- Используйте Chrome или Edge
- Проверьте логи: `docker-compose logs -f backend | grep "🎤"`

### Пересборка с нуля
```bash
docker-compose down -v
docker-compose up --build
```

## 📁 Структура файлов

```
t1/
├── backend/
│   ├── app/
│   │   ├── api/
│   │   │   ├── interview.py    # Основной API интервью
│   │   │   ├── questions.py    # API вопросов
│   │   │   ├── voice.py        # Whisper API
│   │   │   ├── resume.py       # Анализ резюме
│   │   │   └── admin.py        # Админка
│   │   ├── services/
│   │   │   ├── llm_service.py  # Cloud.ru LLM
│   │   │   ├── code_executor.py # Выполнение кода
│   │   │   └── scoring.py      # Подсчёт баллов
│   │   ├── models/             # SQLAlchemy модели
│   │   ├── schemas/            # Pydantic схемы
│   │   └── core/
│   │       ├── config.py       # Конфигурация
│   │       └── database.py     # Подключение к БД
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── LandingPage.tsx    # Главная
│   │   │   ├── InterviewPage.tsx  # Интервью
│   │   │   ├── QuestionsPage.tsx  # Теория
│   │   │   └── ResultPage.tsx     # Результаты
│   │   ├── api/
│   │   │   └── client.ts       # API клиент
│   │   └── styles/             # CSS стили
│   ├── Dockerfile
│   └── package.json
├── deploy/
│   └── docker-compose.yml
├── tasks_base/
│   └── ml_questions.json       # База вопросов
├── README.MD
└── SETUP.md
```

## ⚡ Killer Features

1. **Адаптивная генерация задач** — LLM создаёт задачи под уровень кандидата
2. **Голосовой ввод** — Whisper API для распознавания речи
3. **AI-интервьюер** — диалог, follow-up вопросы, ревью кода
4. **Hint Economy** — подсказки уменьшают максимальный балл
5. **Trust Score** — детекция переключения вкладок
6. **Skill Radar** — визуализация навыков по 5 осям
7. **LLM-оценка теории** — автоматическая проверка ответов

---

**VibeCode** — AI-собеседования нового поколения  
Хакатон T1 2024
